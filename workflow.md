Completely Automated Workflow：

- Upon receiving requirements, documentation, and task details, these are stored directly into Task Genius [or created in the editor].
- [Working on] Automatically create folders, files, and tasks for this item in Task Genius (Using `[[]]` to create file). 
- [Working on/Replace with shell command executor] Use the Terminal in Obsidian to process the task.
- Use AI and MCP to manage both text and tasks.
- [x] Require output in Markdown with variable substitution; the modified document is named `A.md` with frontmatter `typst-script: <script-name>` and `tags: [bon-typst]`. Then Bon^2 automatically generates a Typst document and uses Typst to monitor and produce a PDF.
- After verification, deliver to the client.

Manual Workflow：

- Upon receiving requirements, documentation, and task details, these are stored into Task Genius.
- [Working on] Manually create folders, files, and tasks for this item in Task Genius.
- [Working on/Replace with shell command executor] Use the Terminal in Obsidian to process the task.
- Search for relevant content manually, capture it locally as Markdown using WebClipper or similar tools.
- Ask AI to convert it to an appropriate template.
- [x] Require output in Markdown with variable substitution; the modified document is named `A.md` with frontmatter `typst-script: <script-name>` and `tags: [bon-typst]`. Then Bon^2 automatically generates a Typst document and uses Typst to monitor and produce a PDF.
- After verification, deliver to the client.

-----

完全自动化的工作流：

- 在收到需求、文档和任务详情后，这些信息直接存储到 Task Genius [或在编辑器中创建]。
- [正在处理中] 自动创建文件夹、文件和任务。
- [正在处理中/替换为 shell command executor] 使用 Obsidian 的终端处理任务。
- 使用 AI 和 MCP 管理文本和任务。
- [x] 要求输出为 Markdown 并进行变量替换；修改后的文档名为 `A.md`，frontmatter 为 `typst-script: <script-name>` 和 `tags: [bon-typst]`。然后 Bon^2 自动生成 Typst 文档并使用 Typst 监控和生成 PDF。
- 经过验证后，交付给客户。

手动工作流：

- 在收到需求、文档和任务详情后，这些信息存储到 Task Genius。
- [正在处理中] 手动创建文件夹、文件和任务 (利用 Obsidian 的双链功能)。
- [正在处理中/替换为 shell command executor] 使用 Obsidian 的终端处理任务。
- 手动搜索相关内容，使用 WebClipper 或类似工具捕获本地 Markdown。
- 请求 AI 转换为适当模板。
- [x] 要求输出为 Markdown 并进行变量替换；修改后的文档名为 `A.md`，frontmatter 为 `typst-script: <script-name>` 和 `tags: [bon-typst]`。然后 Bon^2 自动生成 Typst 文档并使用 Typst 监控和生成 PDF。
- 经过验证后，交付给客户。
